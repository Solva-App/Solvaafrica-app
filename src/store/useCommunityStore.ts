// Community store is now handled entirely by React Query (fetchCommunityPosts).
// This file is kept as a compatibility shim in case any legacy code imports it.
// All actual state is managed server-side via the /posts API.

export {};
